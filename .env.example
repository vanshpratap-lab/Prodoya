# Backend is Ruby on Rails (see ./backend). The Vite dev server proxies
# /api/* to the Rails server at http://localhost:3000 (see vite.config.ts).
# No frontend env vars are required. Start Rails first:
#   cd backend && bin/rails server -p 3000
# then:
#   npm run dev
